name: Test

on:
  workflow_call:
  pull_request:
    branches: ["master"]

jobs:
  test:
    uses: ./.github/workflows/reusable-test.yml
    permissions:
      contents: read
      pull-requests: write
