services:
  sbertoactual:
    image: ghcr.io/rvboris/sbertoactual:latest
    container_name: sbertoactual
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
